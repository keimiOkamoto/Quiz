package controllers;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;


public class ServerLinkTest {

    @Test
    public void shouldBeAbleToReturnAQuizServer() {
//        ServerLink serverLink = new ServerLinkImpl();
//        QuizServer actualQuizServer = serverLink.getQuizServer();
//
//        QuizServer expectedQuizServer = mock(QuizServer.class);
//
//        assertEquals(expectedQuizServer, actualQuizServer);
    }
}
